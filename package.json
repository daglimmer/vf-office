{
  "name": "lymph-office",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite public",
    "build": "vite build public --outDir ../dist --emptyOutDir",
    "deploy": "npm run build && cp public/anchors.json public/waypoints.json dist/ && echo 'serve dist VIA the adapter: PUBLIC_DIR=<abs path to dist> PORT=3000 node adapter/index.js'"
  },
  "dependencies": {
    "three": "^0.165.0"
  },
  "devDependencies": {
    "vite": "^5.3.0"
  }
}
