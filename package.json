{
  "name": "lymph-office-phase4",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite public",
    "build": "vite build --config vite.config.js public --base=/office/ --outDir ../dist --emptyOutDir && cp public/office.glb public/waypoints.json dist/",
    "deploy": "npm run build && echo 'copy dist/* into adapter public dir'"
  },
  "dependencies": {
    "three": "^0.165.0"
  },
  "devDependencies": {
    "vite": "^5.3.0"
  }
}
