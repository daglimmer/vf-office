node_modules
dist/
public/public/
adapter/public/
Dockerfile.bak
build.sh
*.log
.env
adapter/state.json

# dev-only local preview config (proxies to live Olympus) — never deploy
vite.dev-live.config.js
